import os
data_dir = 
os.chdir(data_dir)

cell_names = ['SNEDE0000EMT']

for cell_name in cell_names:
    fin_pos = open(cell_name + '_pos', 'r')
    fin_neg = open(cell_name + '_neg', 'r')
    fin_pos_train = open()
    for dna in fin_pos:


    fin.close()